// Este archivo ya no se usa como endpoint de servidor.
// La lógica se movió al cliente (agregar-propiedad.astro).
// Se mantiene vacío para no romper imports existentes.
export const prerender = true;
